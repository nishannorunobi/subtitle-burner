- [x] Update `1_signals-process/start.sh` to create/use venv with `python3` and auto-run `venv.sh` on first run
- [ ] Validate `1_signals-process/venv.sh` installs demucs inside the active venv
- [ ] Re-run Step 2 setup flow and verify `demucs` is available
- [ ] Report test status and remaining coverage
